"infrastructure" -> ServerTypeEnum.infrastructure
// константы типов виртуальных серверов - используются в нескольких сервисах, поэтому хранятся в отдельном классе
// по этим типам проходит фильтрация, и они связываются при первом обращении к данному классу

ticket_lifetime=60 * 60 ->
self.ticket_lifetime = config.ticket_lifetime
ticket_lifetime = self.ticket_lifetime
// время жизни тикета - ранее было захардкожено, теперь берется из конфига, а конфиг - из переменных окружения
// инициализируется при запуске программы и создании объекта класса конфига

self.proxmox_url = config.proxmox_url
// присваивание при инициализации класса, первично - тоже в конфиге
// позволяет быстро перенастраивать приложение при смене URL менеджера виртуальных машин